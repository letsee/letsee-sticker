query getUserByUidQuery($uid: String!) {
  viewer {
    User(uid: $uid) {
      ...CurrentUserFragment
    }
  }
}

fragment CurrentUserFragment on User {
  __typename
  id
  uid
  firstname
  lastname
}
